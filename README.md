# taskmanager
A robust, feature-rich Task Management API built with Django and Django REST Framework (DRF). This backend service is designed to serve as the single source of truth for task data, providing authenticated endpoints for a separate modern frontend application 
